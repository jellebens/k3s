---
- name: Bootstrap SSH access on all nodes
  hosts: all
  gather_facts: false

  roles:
    - role: bootstrap-ssh
      vars:
        ssh_user: "admin"
        ssh_user_groups:
          - "sudo"
        local_pubkey_path: "~/.ssh/id_ed25519.pub"